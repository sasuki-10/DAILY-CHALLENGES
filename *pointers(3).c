#include<stdio.h>
int main()
{
  int *iptr;
  int *fptr;
  int *cptr;
  int *dptr;
  printf("total size of integer value pointer:%d\n",sizeof(iptr));
  printf("total size of float value pointer:%f\n",sizeof(fptr));
  printf("total size of character value pointer:%c\n",sizeof(cptr));
  printf("total size of double value pointer:%d\n",sizeof(dptr));
}
